spring:
  application:
    name: cadastros-jogadores

  sql:
    init:
      mode: always

  datasource:
    url: jdbc:postgresql://localhost:5559/huck
    username: postgres
    password: 123
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  port: 8096